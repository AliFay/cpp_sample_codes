This code implements a stack using linked lists.

The format of the stack's items is in <data value, time stamp> pairs.

The code includes the following functions:

1) Push data into the stack
2) Pop data from the stack
3) Remove data by index
4) Search for an item
5) Remove duplicates and make the stack with unique items

